npx cypress run --spec 'cypress/e2e/**/*.cy.js' --> is the command to run all the tests in head-less mode
OR
npx cypress run  --> runs all cypress specs file. ALL
------------------------
npx cypress open --> for Dashboard